spring:
  datasource:
    url: jdbc:mysql://10.16.2.217:3306/hax?useUnicode=true&characterEncoding=utf8&allowMultiQueries=true&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    show-sql: true
    open-in-view: true # 如果为false：则在transactional结束的时候会关闭connection，如果为true，则在请求返回的时候关闭connection